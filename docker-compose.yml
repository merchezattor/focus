version: '3.8'
services:
  postgres:
    image: postgres:15-alpine
    container_name: todoist-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgrespassword
      POSTGRES_DB: todoist
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 5s
      timeout: 5s
      retries: 5

  web:
    build: .
    container_name: todoist-web
    ports:
      - "3001:3000"
    environment:
      - DATABASE_URL=postgresql://postgres:postgrespassword@postgres:5432/todoist
      - AUTH_SECRET=${AUTH_SECRET}
      - NEXT_PUBLIC_APP_URL=https://todo.michaelukhin.xyz
      - BETTER_AUTH_URL=https://todo.michaelukhin.xyz
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres_data:


